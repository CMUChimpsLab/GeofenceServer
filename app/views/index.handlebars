<h1>{{title}}</h1>

{{> nav}}

<ul>
  {{#each locations}}
      <li class="location">
          <h2>Location Name</h2>
          <p>{{name}}</p>
          <h2>Question about this Location</h2>
          <p>{{question}}</p>
          <h2>Lat</h2>
          <p>{{lat}}</p>
          <h2>Lng</h2>
          <p>{{lng}}</p>
          <h2>Radius</h2>
          <p>{{radius}}</p>
          <br>
          <p>{{formatDate createdAt day="numeric" month="long" year="numeric"}}</p>

          <div class="nav">
              <ul>
                  <li>
                      <a href="{{../routes.DB.TASK_DELETE}}/{{id}}">Delete</a>
                  </li>
              </ul>
          </div>
      </li>
  {{/each}}
</ul>
